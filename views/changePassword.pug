extends layouts/main.pug

block content
    .form-container
        form(action=`/users${routes.changePassword}`, method="post")
            input(type="passworld", name="oldPassword" placeholder="Current Password")
            input(type="passworld", name="newPassword" placeholder="New Password")
            input(type="passworld", name="newPassword1" placeholder="Verify New Password")
            input(type="submit", value="Change Password")
